#include <iostream>
#include <string>

using std::cin;
using std::cout;
using std::endl;
using std ::string;

int main(int argc, char **argv)
{
    int val = 0;
    int num_tries = 0;
    int num_right(0);
    double user_score = 0.0;
    char user_more;
    bool go_for_it = true;
    const int max_tries = 3;

    return 0;
}
